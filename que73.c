#include <stdio.h>
#include <math.h>

int sum(int sum)
{
    int a = 49, b = 81, sum;
    sum = sqrt(a) + sqrt(b);

    return sum;
}

int main()
{
    // int num = ;
    printf("\n %d", sum(sum));
    return 0;
}
